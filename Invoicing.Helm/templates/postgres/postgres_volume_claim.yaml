apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-postgres-volume-claim
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-postgres
spec:
  storageClassName: manual
  volumeName: {{ .Release.Name }}-postgres-volume
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi